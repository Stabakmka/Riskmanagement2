VIEW "com.organon.tocs::GI_QUANTITY_FOR_HU"
AS SELECT 
"DEST_HU_ID",
SUM (CASE WHEN "EVENT_TYPE"='M' THEN CAST("QUANTITY" AS DECIMAL) ELSE -CAST("QUANTITY" AS DECIMAL) END) as "GI_QUANTITY"
FROM "COM_ORGANON_TOCS_EVENTS"
WHERE "EVENT_TYPE"='M' or "EVENT_TYPE"='N'
GROUP BY "DEST_HU_ID"